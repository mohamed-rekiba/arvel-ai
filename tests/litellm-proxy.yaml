# LiteLLM proxy config for the integration environment.
# Route the generic test alias to whichever upstream a key is present for.
model_list:
  - model_name: integration-test
    litellm_params:
      model: anthropic/claude-haiku-4-5
